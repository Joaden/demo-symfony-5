{#{% extends 'base.html.twig' %}#}
{#{% block body %}{{ parent() }}#}
<!-- body navbar fixed-top   style="background-color: #9933ff;"   -->
<main id="main-menu">
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="{{ path('index') }}">Demo Symfony 5</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto" style="text-shadow: 1px 2px 2px #1C6EA4;">
                <li class="nav-item">
                    <!-- route page Acheter un bien avec la seach -->
                    {#<a class="nav-link {% if current_menu is defined and current_menu == 'products' %}active{% endif %}" href="{{ path('product.index') }}">Acheter</a>#}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_user_list') }}">Liste des Utilisateurs !</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Informations
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('about') }}">A Propos de nous</a>
                        <a class="dropdown-item" href="{{ path('infos') }}">Informations</a>
                        <a class="dropdown-item" href="{{ path('privacy') }}">Politique de confidentialité</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('contact') }}">Nous Contacter</a>
                    </div>
                </li>
                {% if not app.user %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('register') }}">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="#" style="text-shadow: 1px 2px 2px #0b29a4;">Bienvenue {{ app.user.username }}</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item" style="text-shadow: 1px 2px 2px #FFBF00;">
                            <a class="nav-link" href="#" style="text-shadow: 1px 2px 2px #FFBF00;">Gérer les biens</a>
                        </li>
                        <li class="nav-item" style="text-shadow: 1px 2px 2px #FFBF00;">
                            <a class="nav-link" href="#" style="text-shadow: 1px 2px 2px #FFBF00;">Gérer les utilisateurs</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a href="#" class="nav-link">Mes messages</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_logout')}}">Se déconnecter</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('account')}}">Mon compte</a>
                    </li>
                {% endif %}
            </ul>
            <!-- <form class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            </form> -->
            <!-- <li class="nav-item">

                <button type="button" class="btn btn-secondary float-right"><a class="nav-link" href="#">Compte</a></button>
            </li> -->
        </div>
    </nav>

</main>
<!-- endbody navbar -->

    {#{% block content %}{% endblock %}#}


{#{% endblock %}#}